services:
  wrenig:
    extends:
      file: ../../base/common-services.yml
      service: wrenig
    volumes:
      - ./config:/srv/wrenig/config
    environment:
      CUSTOM_ENV_VARIABLE: Hello world
